$(document).ready( function () {
    // JS code to change header highlight for team page.
    $('body').find('ul.navbar-nav li').removeClass('active');
    $('body').find('ul.navbar-nav li#team_id').addClass('active');
    // JS code to change header highlight for team page ends.
});